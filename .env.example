# Prisma resolves this relative to the schema folder (`prisma/`) → `prisma/dev.db`
DATABASE_URL="file:dev.db"

# Auth.js (generate: openssl rand -base64 32)
AUTH_SECRET="paste-output-here"
# NEXTAUTH_SECRET="same-as-AUTH-if-you-prefer-nextauth-name"
